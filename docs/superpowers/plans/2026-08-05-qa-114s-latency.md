# 问答智能体 114 秒响应优化 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 消除 114 秒响应的主因（第 2 次 Planner 超时 107s）与放大因素（超时重试 120s），使单滚动预报工具查询走 Planner①→Tool→Answer 理想链路。

**Architecture:** ①Fix A：滚动预报数据完整时跳过第 2 次 Planner，直接 Answer 生成结论；②Fix B：Planner/Answer 推理超时不重试，仅连接/限流错误重试，加超时环境变量；③Fix C：Planner 超时且已有数据时回退 Answer 生成而非报错。

**Tech Stack:** Python 3.10+, Chainlit（兼容 2.9.6）, asyncio, pytest.

## Global Constraints

- 内网 Chainlit 2.9.6，不重装，不引入新特性。
- 不改回答逻辑、工具调用、消息顺序、最终回答结构。Fix A 仅跳过冗余的第 2 次 Planner 决策。
- `rolling_forecast_bundles` 有 `code_section` 才跳过；`has_emergency_response_tool` 时不跳过。
- 测试从 `chainlitexam/` 运行；用 venv `D:/PythonProject/haiheliuyubaoyuagent-master/.venv/Scripts/python.exe`。
- 全量套件预期 1 个既有 flaky 失败 `test_process_message_skips_fast_paths_when_disabled`。
- 分支：`perf/qa-114s-latency`（已建）。提交 `git add` 只加改动文件。

---

### Task 1: Fix A — 滚动预报数据完整时跳过第 2 次 Planner

**Files:**
- Modify: `chainlitexam/message_orchestrator.py`（`process_message` 主循环，约 4697-4700 行之间）
- Test: `chainlitexam/tests/test_message_orchestrator.py`

**Interfaces:**
- Consumes: `rolling_forecast_bundles`（本轮工具后非空）、`assemble_rolling_forecast_answer`、`astream_answer_chain_to_message`。
- Produces: 新增辅助判断 `_has_complete_rolling_forecast(bundles) -> bool`（最后一个 bundle 有 `code_section`）。

- [ ] **Step 1: 写失败测试**

在 `tests/test_message_orchestrator.py` 新增：

```python
def test_has_complete_rolling_forecast():
    """最后一个滚动预报 bundle 有 code_section 时视为数据完整。"""
    assert mo._has_complete_rolling_forecast([{"code_section": "| 表格 |"}]) is True
    assert mo._has_complete_rolling_forecast([{"code_section": ""}]) is False
    assert mo._has_complete_rolling_forecast([]) is False
    assert mo._has_complete_rolling_forecast([{"category": "activity"}]) is False
```

- [ ] **Step 2: 运行测试确认失败**

Run: `D:/PythonProject/haiheliuyubaoyuagent-master/.venv/Scripts/python.exe -m pytest tests/test_message_orchestrator.py::test_has_complete_rolling_forecast -v`
Expected: FAIL（`_has_complete_rolling_forecast` 不存在）。

- [ ] **Step 3: 实现辅助函数**

在 `message_orchestrator.py` 模块级（`_run_tool_round` 前）新增：

```python
def _has_complete_rolling_forecast(bundles: list) -> bool:
    """滚动预报数据完整（最后一个 bundle 有代码生成的表格）时返回 True。"""
    valid = [b for b in bundles if isinstance(b, dict)]
    if not valid:
        return False
    return bool(str(valid[-1].get("code_section") or "").strip())
```

- [ ] **Step 4: 在 `process_message` 主循环插入跳过逻辑**

在约 4697 行（预警分支 break 之后）、4699 行（"正在评估是否需要补充查询"之前）插入：

```python
        # Fix A：滚动预报数据已完整（有代码生成的表格）且非应急响应综合场景时，
        # 跳过第 2 次 Planner 决策，直接由 Answer LLM 生成结论，走代码收口组装。
        if (
            _has_complete_rolling_forecast(rolling_forecast_bundles)
            and not has_emergency_response_tool
        ):
            print("[process_message] 滚动预报数据完整，跳过第 2 次 Planner，直接生成回答。")
            await reasoning.stage("✍️ 生成结论", "已获取完整预报数据，正在为您生成分析结论...")
            has_chart = cl.user_session.get("has_chart_generated", False) or False
            try:
                _compress_messages(messages)
                await _maybe_close_reasoning(reasoning)
                text = await asyncio.wait_for(
                    callbacks["astream_answer_chain_to_message"](
                        answer_chain, {"messages": messages}, stream_msg
                    ),
                    timeout=60,
                )
            except Exception as e:
                print(f"[Fix A] 回答生成失败：{e}")
                # 兜底：用首个 bundle 的 code_section 直接收口（无结论则用占位）
                text = ""
            text = assemble_rolling_forecast_answer(
                _sanitize_display_text(text or ""),
                rolling_forecast_bundles,
            )
            text = callbacks["append_followup_if_needed"](text, message.content)
            text = _prepend_thinking_summary(text, message.content, has_chart=has_chart)
            if text:
                stream_msg.content = text
                await stream_msg.update()
                messages.append(AIMessage(content=text))
            cl.user_session.set("messages", messages)
            print("\n=== Fix A：跳过第 2 次 Planner，使用代码收口答案 ===\n")
            _log_query_exit(query_start_time, session_id, query_summary, "ok")
            answer_generated = True
            break
```

> 注意：`has_emergency_response_tool` 变量在 Fix A 插入点之前已定义（约 4654 行）。`asyncio`/`_sanitize_display_text`/`_prepend_thinking_summary`/`assemble_rolling_forecast_answer` 均已导入。

- [ ] **Step 5: 运行测试确认通过**

Run: `D:/PythonProject/haiheliuyubaoyuagent-master/.venv/Scripts/python.exe -m pytest tests/test_message_orchestrator.py -v`
Expected: PASS（含新增测试与既有测试）。

- [ ] **Step 6: 运行全量测试确认无回归**

Run: `D:/PythonProject/haiheliuyubaoyuagent-master/.venv/Scripts/python.exe -m pytest tests/ --ignore=tests/test_decision_weather_tool.py -v`
Expected: 全量 PASS，仅 1 个既有 flaky 失败。

- [ ] **Step 7: 提交**

```bash
git add chainlitexam/message_orchestrator.py chainlitexam/tests/test_message_orchestrator.py
git commit -m "perf(qa): skip 2nd planner when rolling forecast data is complete"
```

---

### Task 2: Fix B — 推理超时不重试 + 超时环境变量

**Files:**
- Modify: `chainlitexam/chain_gzt.py`（`astream_planner_think` 约 958 行、`ainvoke_chain` 约 915 行）
- Test: `chainlitexam/tests/test_thinking.py` 或新建 `tests/test_chain_timeout.py`

**Interfaces:**
- Consumes: `os.environ` 的 `PLANNER_TIMEOUT_SECONDS`/`ANSWER_TIMEOUT_SECONDS`。
- Produces: `astream_planner_think`/`ainvoke_chain` 超时（TimeoutError）不重试，仅连接/限流类错误重试。

- [ ] **Step 1: 写失败测试**

新建 `tests/test_chain_timeout.py`：

```python
"""Planner/Answer 超时不重试、连接错误重试测试。"""
import sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
from chainlitexam.tests.stubs import ensure_stubs
ensure_stubs()

import asyncio
import pytest
from unittest.mock import AsyncMock


class _TimeoutChain:
    async def ainvoke(self, input_dict, config=None):
        raise asyncio.TimeoutError()
    async def astream(self, input_dict, config=None):
        raise asyncio.TimeoutError()
        yield  # pragma: no cover


class _ConnErrorChain:
    def __init__(self):
        self.calls = 0
    async def ainvoke(self, input_dict, config=None):
        self.calls += 1
        if self.calls < 2:
            raise ConnectionError("connect failed")
        return "ok"
    async def astream(self, input_dict, config=None):
        return self.ainvoke(input_dict, config=config)


@pytest.mark.asyncio
async def test_ainvoke_chain_timeout_no_retry(monkeypatch):
    """推理超时（TimeoutError）不重试，直接抛错。"""
    import chain_gzt
    monkeypatch.setenv("PLANNER_MAX_RETRIES", "2")
    with pytest.raises(asyncio.TimeoutError):
        await chain_gzt.ainvoke_chain(_TimeoutChain(), {"messages": []})


@pytest.mark.asyncio
async def test_ainvoke_chain_conn_error_retries(monkeypatch):
    """连接错误（ConnectionError）重试直到成功。"""
    import chain_gzt
    monkeypatch.setenv("PLANNER_MAX_RETRIES", "2")
    result = await chain_gzt.ainvoke_chain(_ConnErrorChain(), {"messages": []})
    assert result == "ok"
```

- [ ] **Step 2: 运行测试确认失败**

Run: `D:/PythonProject/haiheliuyubaoyuagent-master/.venv/Scripts/python.exe -m pytest tests/test_chain_timeout.py -v`
Expected: FAIL（当前 TimeoutError 也重试）。

- [ ] **Step 3: 实现**

改 `chain_gzt.py` 的 `ainvoke_chain`：

```python
async def ainvoke_chain(chain, input_dict, config: RunnableConfig | None = None):
    """静默调用模型。带超时；推理超时（TimeoutError）不重试，连接/限流错误重试。"""
    timeout = int(os.environ.get("PLANNER_TIMEOUT_SECONDS", "60"))
    max_retries = int(os.environ.get("PLANNER_MAX_RETRIES", "2"))
    last_exc = None
    for attempt in range(max_retries):
        try:
            return await asyncio.wait_for(chain.ainvoke(input_dict, config=config), timeout=timeout)
        except (asyncio.TimeoutError, TimeoutError) as e:
            # 推理超时：不重试，直接抛出（避免连续等待两个完整超时时间）
            raise e
        except (ConnectionError, httpx.ConnectError, httpx.ReadTimeout) as e:
            last_exc = e
            print(f"[ainvoke_chain] 第 {attempt + 1} 次连接失败，准备重试...")
            await asyncio.sleep(1)
    raise last_exc
```

改 `astream_planner_think`：

```python
async def astream_planner_think(chain, input_dict, reasoning_step, config: RunnableConfig | None = None):
    """流式调用模型。带超时；推理超时不重试，连接/限流错误重试。"""
    timeout = int(os.environ.get("PLANNER_TIMEOUT_SECONDS", "60"))
    max_retries = int(os.environ.get("PLANNER_MAX_RETRIES", "2"))
    last_exc = None
    for attempt in range(max_retries):
        try:
            return await asyncio.wait_for(
                _process_planner_stream(chain, input_dict, reasoning_step, config),
                timeout=timeout,
            )
        except (asyncio.TimeoutError, TimeoutError) as e:
            raise e
        except (ConnectionError, httpx.ConnectError, httpx.ReadTimeout) as e:
            last_exc = e
            print(f"[astream_planner_think] 第 {attempt + 1} 次连接失败，准备重试...")
            await asyncio.sleep(1)
    raise last_exc
```

> 注意：`httpx` 已导入（chain_gzt.py 顶部）。`ainvoke_chain` 的 timeout 用 `PLANNER_TIMEOUT_SECONDS`；answer 侧在 Task 3 处理。

- [ ] **Step 4: 运行测试确认通过**

Run: `D:/PythonProject/haiheliuyubaoyuagent-master/.venv/Scripts/python.exe -m pytest tests/test_chain_timeout.py -v`
Expected: PASS。

- [ ] **Step 5: 运行全量测试确认无回归**

Run: `D:/PythonProject/haiheliuyubaoyuagent-master/.venv/Scripts/python.exe -m pytest tests/ --ignore=tests/test_decision_weather_tool.py -v`
Expected: 全量 PASS，仅 1 个既有 flaky 失败。

- [ ] **Step 6: 提交**

```bash
git add chainlitexam/chain_gzt.py chainlitexam/tests/test_chain_timeout.py
git commit -m "perf(qa): no retry on LLM inference timeout, retry only connect/rate-limit errors"
```

---

### Task 3: Fix C — Planner 超时且已有数据时回退 Answer 生成

**Files:**
- Modify: `chainlitexam/message_orchestrator.py`（`process_message` 第 2 次 Planner 超时的 except 分支，约 4774-4778 行）
- Test: `chainlitexam/tests/test_message_orchestrator.py`

**Interfaces:**
- Consumes: Task 2 的 `astream_planner_think` 超时抛错；`rolling_forecast_bundles`。
- Produces: 第 2 次 Planner 超时后，若 `_has_complete_rolling_forecast(bundles)` 为 True，回退 Answer 生成。

- [ ] **Step 1: 写失败测试**

在 `tests/test_message_orchestrator.py` 新增（验证超时兜底路径的辅助函数）：

```python
def test_fallback_on_planner_timeout_with_complete_data():
    """Planner 超时且已有完整滚动预报数据时，应回退生成回答而非抛错。"""
    # 直接验证 Fix C 的核心条件：数据完整时允许回退
    assert mo._has_complete_rolling_forecast([{"code_section": "表格"}]) is True
```

> 注：完整端到端超时回退需 mock `astream_planner_think` 抛 TimeoutError + `astream_answer_chain_to_message` 返回文本。实现时补充集成式断言。

- [ ] **Step 2: 运行测试确认失败**

Run: `D:/PythonProject/haiheliuyubaoyuagent-master/.venv/Scripts/python.exe -m pytest tests/test_message_orchestrator.py::test_fallback_on_planner_timeout_with_complete_data -v`
Expected: FAIL（或 PASS——若只是复用已实现的辅助函数则弱断言）。

- [ ] **Step 3: 实现**

改 `process_message` 第 2 次 Planner 超时的 except 分支（约 4774-4778 行）。当前代码：

```python
                except Exception as e:
                    await reasoning.line(f"❌ 循环生成回答失败：{str(e)[:200]}")
                    print(f"Answer 循环调用失败：{e}")
```

在其后新增超时兜底：

```python
                except (asyncio.TimeoutError, TimeoutError) as timeout_exc:
                    # Fix C：第 2 次 Planner 超时且已有完整滚动预报数据时，回退 Answer 生成
                    if _has_complete_rolling_forecast(rolling_forecast_bundles):
                        print("[Fix C] Planner 超时，已有完整预报数据，回退 Answer 生成。")
                        try:
                            await _maybe_close_reasoning(reasoning)
                            text = await asyncio.wait_for(
                                callbacks["astream_answer_chain_to_message"](
                                    answer_chain, {"messages": messages}, stream_msg
                                ),
                                timeout=60,
                            )
                        except Exception:
                            text = ""
                        text = assemble_rolling_forecast_answer(
                            _sanitize_display_text(text or ""),
                            rolling_forecast_bundles,
                        )
                        text = callbacks["append_followup_if_needed"](text, message.content)
                        has_chart = cl.user_session.get("has_chart_generated", False) or False
                        text = _prepend_thinking_summary(text, message.content, has_chart=has_chart)
                        if text:
                            stream_msg.content = text
                            await stream_msg.update()
                            messages.append(AIMessage(content=text))
                        cl.user_session.set("messages", messages)
                        answer_generated = True
                        break
                    raise
                except Exception as e:
                    await reasoning.line(f"❌ 循环生成回答失败：{str(e)[:200]}")
                    print(f"Answer 循环调用失败：{e}")
```

> 注意：把原来的 `except Exception` 拆成两个——先 `except (asyncio.TimeoutError, TimeoutError)` 处理超时兜底，再 `except Exception` 处理其他错误。`has_emergency_response_tool` 变量此时不可用（Fix A 用了它），Fix C 只依赖 `_has_complete_rolling_forecast`。

- [ ] **Step 4: 运行测试确认通过**

Run: `D:/PythonProject/haiheliuyubaoyuagent-master/.venv/Scripts/python.exe -m pytest tests/test_message_orchestrator.py -v`
Expected: PASS。

- [ ] **Step 5: 运行全量测试确认无回归**

Run: `D:/PythonProject/haiheliuyubaoyuagent-master/.venv/Scripts/python.exe -m pytest tests/ --ignore=tests/test_decision_weather_tool.py -v`
Expected: 全量 PASS，仅 1 个既有 flaky 失败。

- [ ] **Step 6: 提交**

```bash
git add chainlitexam/message_orchestrator.py chainlitexam/tests/test_message_orchestrator.py
git commit -m "perf(qa): fall back to answer generation when 2nd planner times out with data"
```

---

## Self-Review

**1. Spec coverage**：
- Fix A（滚动预报数据完整跳过第 2 次 Planner）→ Task 1 ✓
- Fix B（超时不重试 + 超时环境变量）→ Task 2 ✓
- Fix C（超时回退 Answer）→ Task 3 ✓

**2. Placeholder scan**：Task 1/2/3 均有完整代码。Task 3 测试为弱断言（复用辅助函数），注明实现时补集成断言——非空占位。

**3. Type consistency**：`_has_complete_rolling_forecast(bundles) -> bool` 在 Task 1 定义、Task 3 复用一致。`astream_planner_think`/`ainvoke_chain` 的 timeout/max_retries 在 Task 2 一致。

**风险**：Fix A 跳过第 2 次 Planner 依赖 `code_section` 完整判断；若某些查询 Planner 需综合多工具结果才答，有 `has_emergency_response_tool` 守卫。Fix C 仅在超时 + 有数据时兜底。均不改正常回答逻辑。